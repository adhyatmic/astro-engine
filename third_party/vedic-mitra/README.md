# Frozen `:core:astronomy` + `:core:common` (engine-only)

| File | Meaning |
|------|---------|
| `UPSTREAM_COMMIT.txt` | Upstream git SHA at freeze |
| `FROZEN_AT.txt` | UTC freeze time |
| `astronomy-kotlin/` | Full astronomy main sources (no `di/`) |
| `common-kotlin/` | Common deps used by astronomy (no `di/`) |

**Not present:** `feature/*` UI modules.
